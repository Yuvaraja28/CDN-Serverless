# CDN Server Configuration
# Copy this to .env and fill in the values

# JSON object mapping path prefixes to upstream configuration
ROUTES_JSON='{"/prefix": {"upstreamHostname": "origin.com", "upstreamPathPrefix": "folder", "requiredRefererHostname": "site.com", "rateLimiterKey": "BINDING_NAME"}}'

# Cache TTL for downstream clients in seconds
CACHE_TTL=7200
